{
  "name": "conway-app",
  "version": "0.1.0",
  "description": "A browser interface to Conway's Game of Life.",
  "author": "Dustin Rohde",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "browserify": "^16.2.2",
    "chokidar-cli": "^1.2.0",
    "watchify": "^3.11.0"
  },
  "scripts": {
    "lint": "eslint **/*.js",
    "clean": "rm -rf dist/* && mkdir -p dist/vendor/",
    "ls-assets": "find src/* -maxdepth 0 ! -name  '*.js' | xargs",
    "build-assets": "npm run clean && cp -rf $(npm run --silent ls-assets) dist/",
    "build-js": "browserify src/index.js > dist/main.js",
    "build": "npm run build-assets && npm run build-js",
    "watch-assets": "chokidar $(npm run --silent ls-assets) --silent -c 'cp -rvf {path} dist/'",
    "watch-js": "watchify src/index.js -v --outfile dist/main.js",
    "watch": "npm run watch-assets & npm run watch-js"
  },
  "main": "src/index.js"
}
